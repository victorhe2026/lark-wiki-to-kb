# Core (CLI)
openai>=1.40
pyyaml>=6.0
numpy>=1.24

# GUI (optional) — also installable via:  pip install '.[gui]'
fastapi>=0.110
uvicorn>=0.29
pywebview>=5.0

# Local embeddings (optional) — on-device vector search, no embedding API needed.
# Also installable via:  pip install '.[local-embed]'
fastembed>=0.3
